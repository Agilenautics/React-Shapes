mutation CreateProjects($input: [ProjectCreateInput!]!) {
  createProjects(input: $input) {
    projects {
      id
      name
      description
      recycleBin
      timeStamp
      usersInProjects {
        emailId
      }
    }
  }
}
